<%= turbo_frame_tag "paste_title_#{paste.id}" do%>
  <%= link_to paste.title, paste_path(paste), 
                  data: { turbo_frame: "_top" },
                  class: "text-lg font-medium text-gray-900 hover:underline" %>
  <%= link_to edit_title_paste_path(paste), 
                    data: { turbo_frame: "paste_title_#{paste.id}" },
                    class: "ml-2 text-gray-500 hover:text-gray-600 focus:outline-none" do %>
    <i class="fas fa-edit"></i>
  <% end %>
<% end %>